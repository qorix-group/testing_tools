# *******************************************************************************
# Copyright (c) 2025 Contributors to the Eclipse Foundation
#
# See the NOTICE file(s) distributed with this work for additional
# information regarding copyright ownership.
#
# This program and the accompanying materials are made available under the
# terms of the Apache License Version 2.0 which is available at
# https://www.apache.org/licenses/LICENSE-2.0
#
# SPDX-License-Identifier: Apache-2.0
# *******************************************************************************
name: Build and test scenarios

on:
  pull_request:
    types: [opened, reopened, synchronize]
  push:
    branches: [main]

jobs:
  scenarios-build:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Bazel
        uses: bazel-contrib/setup-bazel@0.19.0
        with:
          bazelisk-version: 1.26.0 # newest LTS before 1 Jun 2025
          bazelisk-cache: true
          repository-cache: true
          disk-cache: true
          cache-version: scenarios-build
          # Restore on every run, but only write caches from pushes to main.
          cache-save: ${{ github.event_name == 'push' }}

      - name: Run build
        run: |
          bazel build //... --config=x86_64-linux

      - name: Run tests
        run: |
          bazel test //... --config=x86_64-linux

  clang-tidy:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Bazel
        uses: bazel-contrib/setup-bazel@0.19.0
        with:
          bazelisk-version: 1.26.0
          bazelisk-cache: true
          repository-cache: true
          disk-cache: true
          cache-version: clang-tidy
          # Restore on every run, but only write caches from pushes to main.
          cache-save: ${{ github.event_name == 'push' }}

      - name: Run clang-tidy
        run: |
          bazel test --config=clang-tidy //score/test_scenarios_cpp:clang_tidy

  clippy:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@1.92.0
        with:
          components: clippy

      - name: Run clippy
        working-directory: score/test_scenarios_rust
        run: |
          cargo clippy --locked --all-targets -- -D warnings
